const Destinatary = function (destinatary) {
  this.destinatary_rut = destinatary.destinatary_rut;
  this.destinatary_name = destinatary.destinatary_name;
  this.destinatary_mail = destinatary.destinatary_mail;
  this.destinatary_telephone = destinatary.destinatary_telephone;
  this.destinatary_typeAccount = destinatary.destinatary_typeAccount;
  this.destinatary_numberAccount = destinatary.destinatary_numberAccount;
};

module.exports = Destinatary;
